# PostgreSQL & Database Design Skill
- 3NF Normalized schema with indexed foreign keys.
- Audit fields (CreatedAtUtc, UpdatedAtUtc, IsDeleted) and EF Core migrations.
- Transaction management and seed data generation.
